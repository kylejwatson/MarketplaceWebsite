<?php require('template/header.phtml') ?>

    <h3>Search Adverts</h3>

    <div id="formcont" class="col-sm-9">
        <form class="form-horizontal"  enctype="multipart/form-data" id="searchform">
            <div class="form-group">
                <label class="control-label col-sm-4" for="title">Keywords: </label>
                <div class="col-sm-8">
                    <input type="text" class="form-control" id="title" name="title" value="<?php if(isset($view->title))echo $view->title; ?>"/>
                </div>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-4" for="minprice">Min-price: </label>
                <div class="col-sm-8">
                    <input type="number" step="0.01" min="0" max="9999.99" name="minprice" value="<?php if(isset($view->min))echo $view->min; ?>" class="form-control" id="minprice"/>
                </div>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-4" for="minprice">Max-price: </label>
                <div class="col-sm-8">
                    <input type="number" step="0.01" min="0" max="9999.99" name="maxprice" value="<?php if(isset($view->max))echo $view->max; ?>" class="form-control" id="maxprice"/>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-4 col-sm-8">
                    <div class="checkbox">
                        <label><input type="checkbox" name="digital" <?php if(isset($view->dig)){if($view->dig) echo "checked";}else echo "checked"; ?>> Digital</label>
                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-4 col-sm-8">
                    <div class="checkbox">
                        <label><input type="checkbox" name="notdigital" <?php if(isset($view->not)){if($view->not) echo "checked";}else echo "checked"; ?>> Physical</label>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-4 col-sm-8">
                    <input id="submit-btn" class="btn btn-default" type="button" value="Search" name="submit" onclick="search()"/>
                </div>
            </div>
        </form>
    </div>

    <script src="../js/infinitescroll.js"></script>
    <script type="text/javascript">
        load();
        var formdata;
        function search(){
            document.getElementById("adspace").innerHTML = "";
            formdata = new FormData(document.getElementById("searchform"));
            off = 1;
            document.getElementById("formcont").className = "col-sm-3";
            loadAds()
        }
        function loadAds() {
            var xhr = new XMLHttpRequest();
            xhr.open('POST', 'searchnextads.php', true);
            //xhr.setRequestHeader("Content-type","");
            xhr.onreadystatechange = function () {
                var DONE = 4;
                var OK = 200;
                if (xhr.readyState == DONE) {
                    document.getElementById("adspace").innerHTML += xhr.responseText;
                    console.log("off rec" + off);
                    send = true;
                }
                //console.log(xhr.responseText);
            }
            formdata.set("offset",off);
            xhr.send(formdata);
            console.log("off send" + off);
            off++;
        }
    </script>
    <div class="col-sm-9">
        <ul>
            <div id="adspace" class="row">

            </div>
        </ul>
    </div>

<?php require('template/footer.phtml'); ?>